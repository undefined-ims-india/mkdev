

import React, { ReactElement } from 'react';
import Nav from './Nav'

const Login = (): ReactElement => {
  return (
    <>
 <h2>Login page</h2>
 <h4> Will be adding the auth routing here </h4>
 <br/>
 <h4>Here are the routes to navigate for now</h4>
 <>
 <Nav />
 </>
    </>
  );
};

export default Login;
