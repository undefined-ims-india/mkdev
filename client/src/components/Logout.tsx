import React, { ReactElement } from 'react';

const Logout = (): ReactElement => {
  return (
    <>
      <h2>You have successfully logged out!</h2>
      <h3>Refreshing automatically routes back to App page...</h3>
    </>
  );
};

export default Logout;
